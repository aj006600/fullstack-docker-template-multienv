server {
    listen 80;

    # SPA 靜態檔（找不到路徑就回 index.html，交給前端路由）
    location / {
        root /usr/share/nginx/html;
        try_files $uri /index.html;
    }

    # /api 反向代理到 backend 服務（compose 內以服務名 backend 解析）
    location /api/ {
        proxy_pass http://backend:8000;
    }
}
