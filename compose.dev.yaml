# 本機開發 override：make dev → localhost:3000 / :8000，後端 hot reload。
services:
  backend:
    build:
      context: ./backend
      target: dev            # dev stage 含 pytest/ruff；啟動指令（--reload）由該 stage 的 CMD 提供
    volumes:
      - ./backend/app:/app/app
      - ./backend/tests:/app/tests
    ports:
      - "8000:8000"

  frontend:
    ports:
      - "3000:80"
