name: CI/CD

on:
  push:
    branches: [main]
    paths-ignore:          # 純文件變更不觸發 build/deploy（PR 仍會跑測試）
      - '**.md'
      - 'docs/**'
    tags: ['v*']           # 打 v* tag → 發版到 prod
  pull_request:
    branches: [main]

env:
  IMAGE: ghcr.io/${{ github.repository }}

jobs:
  # ---------- CI：後端測試（PR / main / tag 都跑）----------
  test-backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: uv sync --frozen --dev
      - run: uv run pytest -q

  # ---------- CI：前端建置檢查 ----------
  build-frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npm run build

  # ========== merge 到 main：build 兩個映像 + 自動部署 dev → qas（不含 prod）==========
  build:
    needs: [test-backend, build-frontend]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        service: [backend, frontend]
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: ./${{ matrix.service }}
          push: true
          tags: ${{ env.IMAGE }}-${{ matrix.service }}:${{ github.sha }}

  deploy-dev:
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - run: echo "部署 backend+frontend :${{ github.sha }} 到 dev"
      # TODO: 換成實際部署指令

  deploy-qas:
    needs: deploy-dev
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: qas
    steps:
      - run: echo "部署 backend+frontend :${{ github.sha }} 到 qas"
      # TODO: 換成實際部署指令

  # ========== 打 v* tag：發版到 prod（兩個映像加版本標籤，不重 build）==========
  release:
    needs: [test-backend, build-frontend]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        service: [backend, frontend]
    steps:
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      # 不重 build：把 tag 指到的 commit 的 :sha 映像，加上版本標籤（build-once）
      - run: |
          docker buildx imagetools create \
            --tag ${{ env.IMAGE }}-${{ matrix.service }}:${{ github.ref_name }} \
            ${{ env.IMAGE }}-${{ matrix.service }}:${{ github.sha }}

  deploy-prod:
    needs: release
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    environment: production   # 在 GitHub 設 required reviewers → 上 prod 需人工核准
    steps:
      - run: echo "發版 backend+frontend :${{ github.ref_name }}（= :${{ github.sha }}）到 prod"
      # TODO: 換成實際部署指令
